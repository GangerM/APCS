import java.util.*;
import java.io.File;
import java.util.ArrayList;
import java.io.IOException;
public class Statistics
{   private ArrayList <Integer> numbers;
    private Scanner in;
    public Statistics(){
        numbers = new ArrayList <Integer> ();
         File file = new File("C:/Users/Mitul/Desktop/numbers.txt");
        try{
            in = new Scanner(file);
            while(in.hasNext()){
                numbers.add(in.nextInt());
            }
        }
        catch(IOException e){
            System.out.println(e.getMessage());
        }
       
    }
    
    public void sDeviation(){
        int sum = 0;
        double avg = findAverage();
        for (Integer i:numbers){
            sum+= Math.pow(i-avg,2);
        }
        System.out.printf("%.2f", Math.sqrt(sum/(numbers.size()-1)));
    }
    
    public float findSum(){
    float sum = 0;
    for(int x = 0; x < numbers.size(); x++){
    sum += numbers.get(x);
    }
    return sum;
    }
    
    public int size(){
    return numbers.size(); // gets the size of the array aka how many numbers there are
    }
    
    public int mode(){
        int mode = 0;
        int modeCount = 0;
        
        for(int i=0;  i< numbers.size();i++){
            int x = numbers.get(i);
            int tempCount = 1;
            
             for(int y : numbers){
              int x2 = numbers.get(y);
                if( x == x2){
                    tempCount++;
                }
                if( tempCount > modeCount){
                    modeCount = tempCount;
                    mode = x;
                }
            }
        }
        return mode;
    } 
    
    public double findAverage(){
    return findSum() / size();
    }
}
