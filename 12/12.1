
public class FunLoops
{   
    public boolean checkSquares(int x){
    int y = 1;
    while((x*x) > 0 ){
        if (sumOf(x) == (x*x)){
            
        }
    }
    return false;
    }
    public int sumOf(int x){
    if (x == 1)
        return 1;
    else return x + sumOf(x-1);
    }
    public int MagicSquare(int n){
    int x = 1;
    while ( n > 0){
        if ((checkSquares(x) == true)){
            n--;
            return x;
        }
        else x++;
    }
    }
    public int LCM(int l)
    {
    
    }
}
